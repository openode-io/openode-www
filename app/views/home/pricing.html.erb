<section class="pricing-header bg-light px-3 py-3 pt-md-5 pb-md-4 mx-auto">
  <div class="container cointaier-shrinked">
    <h1 class="display-4 text-left text-success">Subscription Plans</h1>
    <h2 class="text-muted text-left">All inclusive and recurring plans!</h2>
    <p class="lead text-left">The Pro subscription plan comes with the innovative <i>Auto Memory Allocation</i> feature, which allocates the proper memory for your instance automatically.</p>

    <p>You select any number of instances which are available for you to use. All included and no extra fee. Easy.</p>
  </div>
</section>
<section class="pt-8 pt-md-11 bg-dark p-3 p-md-5">
  <div class="container">    
    <div class="card-deck mb-3 text-center">
      <div class="row">
      <% @pricing_plans.each do |pricing| %>
        <div class="col-sm-12 col-12 col-md-5 col-lg-5 mb-7">
          <div class="card border-success shadow-sm ml-0 mr-0 h-100 hvr-grow">
            <div class="card-header bg-success text-white">
              <h4 class="my-0 font-weight-normal"><%= pricing[:name] %></h4>
            </div>
            <div class="card-body">
              <h1 class="card-title pricing-card-title"><%= pricing[:title] %></h1>
              <h3><small class="text-muted"><%= pricing[:subtitle] %></small></h3>
              <ul class="list-unstyled mt-3 mb-4">
                <% pricing[:features].each do |feature|  %>
                  <li class="text-center mb-2 mt-2 pt-2 pb-3 border-bottom"><%= feature %></li>
                <% end %>
              </ul>            
            </div>
            <div class="card-footer">
              <% if current_user %>
                <a href="/admin/billing/pay?plan=<%= pricing[:id] %>"
                    class="btn btn-lg btn-block btn-outline-success">
                  Order
                </a>
              <% else %>
                <a href="/users/register"
                    class="btn btn-lg btn-block btn-outline-success">
                  Sign Up
                </a>
              <% end %>
              
            </div>
          </div>
        </div>
      <% end %> 
      </div>
    </div>      
  </div>
</section>
<%= render 'home/sections/features_header' %>
<%= render 'home/sections/features_list' %>
<section class="border-bottom"></section>
